# JWT Secret for Authentication
# Generate a secure random string for production
JWT_SECRET=your_jwt_secret_key_here

# Contact form email delivery (required in production)
CONTACT_RECIPIENT_EMAIL=support@yegnabiz.com
SMTP_FROM_EMAIL=YegnaBiz <no-reply@yegnabiz.com>
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp_username
SMTP_PASS=smtp_password
# Set to "true" for port 465 or other TLS-only providers
SMTP_SECURE=false

# Database Configuration for Prisma
# For Prisma Accelerate (Edge Runtime)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=your_api_key_here"

# Alternative: Standard PostgreSQL connection (if not using Accelerate)
# DATABASE_URL="postgresql://username:password@localhost:5432/yegnabiz?schema=public"
